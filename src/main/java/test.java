import PIU.PIU;
import PIU.utilities.Logger;

public class test {
    public static void main(String[] args) {

        Logger logger = Logger.getInstance(); // or set
        // Step 1 - get vals to insert (create files / file (json))
        // Step 2 - when ask to submit
        PIU.main(args);


    }
}
